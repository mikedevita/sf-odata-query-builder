{
  "name": "sap-sfec-browser",
  "version": "0.1.0",
  "description": "SAP SuccessFactors EC OData Query Builder",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "electron:dev": "cross-env NODE_ENV=development electron .",
    "generate-icons": "node scripts/generate-icons.js",
    "electron:build": "node scripts/generate-icons.js && vite build && electron-builder",
    "docker:build": "docker build -t sap-sfec-browser .",
    "docker:run": "docker run -p 3000:3000 sap-sfec-browser"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "cross-env": "^10.1.0",
    "electron": "^42.4.1",
    "electron-builder": "^26.15.3",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "express": "^4.19.0"
  },
  "build": {
    "appId": "co.devita.sap-sfec-browser",
    "productName": "SAP SFEC Browser",
    "files": [
      "dist/**/*",
      "electron/**/*.cjs"
    ],
    "directories": {
      "output": "release"
    },
    "icon": "public/icon.png",
    "win": {
      "target": "nsis"
    },
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": "AppImage"
    }
  }
}
